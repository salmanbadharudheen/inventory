{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "echo '=== MIGRATE ===' && python manage.py migrate --noinput && echo '=== LOAD DATA ===' && python load_initial_data.py && echo '=== STARTING GUNICORN ===' && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --timeout 120 --workers 2 --access-logfile - --error-logfile -",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
